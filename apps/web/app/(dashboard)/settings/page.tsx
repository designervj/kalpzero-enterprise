'use client';

// The actual Control Center grid UI is rendered by `settings/layout.tsx`.
// This page simply exists so Next.js resolves the `/settings` route.
// When the user is on `/settings`, the layout checks `isHubView` and renders the Navigation Grid.

export default function SettingsHubPage() {
    return null;
}
